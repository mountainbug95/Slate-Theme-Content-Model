---
label: Home
hide_body: true
is_partial: false
fields:
  - type: text
    name: title
    label: Page Title
    config:
      required: true
  - type: field_group
    name: pagination
    label: Pagination
    fields:
      - type: boolean
        name: enabled
        label: Enable Pagination
        description: Enables pagination of the post section on the home page
        default: false
  - type: field_group_list
    name: sections
    label: Sections
    description: Home Sections
    config:
      labelField: identifier
    fields:
      - type: include
        name: include_section
        label: Section
        template: section
  - type: text
    name: layout
    label: Layout
    hidden: true
    default: home
    config:
      required: true
pages:
  - index.md
